<% @item = @project.name %>
<h2><%= @item %></h2>
<div id="project">
    <%= link_to 'Edit Project', edit_project_path(@project) %><br>
    <%= link_to 'Delete Project', @project, :method => 'delete', :confirm => 'Are you sure you want to delete this project?' %><br>
    <%= link_to 'New Ticket', new_project_ticket_path(@project) %>
</div>
